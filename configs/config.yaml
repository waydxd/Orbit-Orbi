# Orbi Configuration

# Calendar service gRPC address
calendar_service_addr: "localhost:50051"

# OpenAI API configuration
openai_api_key: "your-openai-api-key-here"
openai_model: "gpt-3.5-turbo"

# Agent configuration
max_iterations: 5
timeout_seconds: 30
